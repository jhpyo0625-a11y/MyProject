# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
*.egg-info/

# Data (large binary files - track manifests/index CSVs, not images or embeddings)
data/crops/
data/embeddings/**/*.npy
data/labels.db
data/prediction_log.csv
data/raw/
Coil-image-Dataset/

# Generated previews (scratch files)
preview_*.png
raw_preview_*.png

# Archived models (track production snapshot only)
models/archive/

# Retrain candidate (transient, 63MB) + operational state
models/candidate/
models/production/retrain_state.json

# PaDiM detector artifact (61MB, regenerable via `python -m src.training.anomaly`)
models/production/padim.pkl

# MLflow run data
experiments/
mlruns/

# Generated per-run retrain comparison reports (timestamped, regenerable)
reports/model_comparison_*.md

# Scratch exploration plots (regenerable)
reports/aug_check.png
reports/crop_compare.png
reports/one_sample.png
reports/fullframe_miss_*.png
reports/newcrop_check.png
reports/diagnose_fold4_missed.png

# Sample / masked images dropped in project root (not dataset)
/Pass.bmp
/Dent.bmp
/Loose.bmp
/real-*.bmp

# Claude local settings
.claude/

# IDE / OS
.idea/
.vscode/
*.swp
Thumbs.db
.DS_Store
