# ── PyTorch (CPU-only) ────────────────────────────────────────────────────────
# Install separately before the rest:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
#
# If wheels for your Python version are unavailable on that index, try nightly:
#   pip install --pre torch torchvision --index-url https://download.pytorch.org/whl/nightly/cpu
#
torch
torchvision

# ── ML & vision ───────────────────────────────────────────────────────────────
timm>=1.0.0
scikit-learn>=1.4.0
opencv-python>=4.9.0
Pillow>=10.0.0
numpy>=1.26.0

# ── Data & experiment tracking ─────────────────────────────────────────────────
pandas>=2.0.0
mlflow>=2.12.0
PyYAML>=6.0

# ── UI ────────────────────────────────────────────────────────────────────────
PyQt5>=5.15.0
